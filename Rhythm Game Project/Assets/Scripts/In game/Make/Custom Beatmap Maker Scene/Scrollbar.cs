using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

namespace CustomBeatmapMaker
{
/*    public class ScrollbarScript : ClickableUI
    {
        public NoteContainer Container;
        public CameraScript Camera;

        public void UpdateValue()
        {
            GetScrollbar().value = Camera.transform.position.y / Container.HighestNote;
        }

        public void UpdateCamera()
        {
            var pos = Camera.transform.position;
            float newY = GetScrollbar().value * Container.HighestNote;
            Camera.transform.position = new Vector3(pos.x, newY, pos.z);
        }

        public Scrollbar GetScrollbar()
        {
            return gameObject.GetComponent<Scrollbar>();
        }
    }*/
}